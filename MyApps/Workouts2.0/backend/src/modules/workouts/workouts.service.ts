import { Injectable, NotFoundException } from '@nestjs/common';
import { PrismaService } from '../../shared/prisma/prisma.service';

const MS_PER_DAY = 24 * 60 * 60 * 1000;
const DAY_LABELS: Record<number, string> = { 0: 'Sunday', 1: 'Monday', 2: 'Tuesday', 3: 'Wednesday', 4: 'Thursday', 5: 'Friday', 6: 'Saturday' };
function startOfDay(date: Date) { const d = new Date(date); d.setHours(0, 0, 0, 0); return d; }
function calculateWeekIndex(plan: { createdAt: Date; currentWeekIndex: number; startDate?: Date | null; durationWeeks?: number | null }, today = new Date()) { const anchor = plan.startDate ?? plan.createdAt; const daysSinceStart = Math.floor((startOfDay(today).getTime() - startOfDay(anchor).getTime()) / MS_PER_DAY); const weekIndex = Math.max(0, Math.floor(daysSinceStart / 7)); const maxWeek = Math.max(0, Number(plan.durationWeeks ?? 1) - 1); return Math.min(weekIndex, maxWeek); }
function normalizeExerciseCanonicalName(name: unknown) { return typeof name === 'string' ? name.trim().replace(/\s+/g, ' ') : ''; }
const EXERCISE_LIBRARY: Record<string, any> = { 'Barbell Back Squat': { strategy: 'weight', weight: { type: 'percent', value: 0.025, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Barbell Bench Press': { strategy: 'weight', weight: { type: 'percent', value: 0.025, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Barbell Deadlift': { strategy: 'weight', weight: { type: 'percent', value: 0.02, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Overhead Press': { strategy: 'weight', weight: { type: 'percent', value: 0.025, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Incline Dumbbell Press': { strategy: 'weight', weight: { type: 'increment', value: 2, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Dumbbell Shoulder Press': { strategy: 'weight', weight: { type: 'increment', value: 2, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Dumbbell Row': { strategy: 'weight', weight: { type: 'increment', value: 2, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Seated Cable Row': { strategy: 'weight', weight: { type: 'increment', value: 2.5, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Lat Pulldown': { strategy: 'weight', weight: { type: 'increment', value: 2.5, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Romanian Deadlift': { strategy: 'weight', weight: { type: 'percent', value: 0.02, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Leg Press': { strategy: 'weight', weight: { type: 'increment', value: 5, decimals: 1 }, reps: { type: 'increment', value: 0, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Lunge': { strategy: 'hybrid', weight: { type: 'increment', value: 1, decimals: 1 }, reps: { type: 'increment', value: 1, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Bulgarian Split Squat': { strategy: 'hybrid', weight: { type: 'increment', value: 1, decimals: 1 }, reps: { type: 'increment', value: 1, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Pull-Up': { strategy: 'bodyweight', weight: { type: 'increment', value: 0, decimals: 1 }, reps: { type: 'increment', value: 1, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Bicep Curl': { strategy: 'reps', weight: { type: 'increment', value: 1, decimals: 1 }, reps: { type: 'increment', value: 1, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } }, 'Tricep Pushdown': { strategy: 'reps', weight: { type: 'increment', value: 1.25, decimals: 1 }, reps: { type: 'increment', value: 1, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } } };
const GENERIC_PROGRESSIVE_OVERLOAD_RULE = { strategy: 'generic', weight: { type: 'percent', value: 0.025, decimals: 1 }, reps: { type: 'increment', value: 1, min: 1 }, sets: { type: 'increment', value: 0, min: 1 } };
function getExerciseProgressionRule(exerciseName: unknown) { return EXERCISE_LIBRARY[normalizeExerciseCanonicalName(exerciseName)] ?? GENERIC_PROGRESSIVE_OVERLOAD_RULE; }
function applyProgression(exercise: any, weekIndex: number, enabled: boolean) { if (!enabled || weekIndex <= 0) return exercise; const rule = getExerciseProgressionRule(exercise.exerciseName); const setsBase = Number(exercise.setsTarget ?? 0); const repsBase = Number(exercise.repsTarget ?? 0); const weightBase = typeof exercise.weightTarget === 'number' ? exercise.weightTarget : null; const rounds = weekIndex; let setsTarget = Math.max(1, setsBase); let repsTarget = Math.max(1, repsBase); let weightTarget = weightBase; if (rule.sets?.type === 'increment') setsTarget = Math.max(rule.sets.min ?? 1, setsBase + rule.sets.value * rounds); if (rule.reps?.type === 'increment') repsTarget = Math.max(rule.reps.min ?? 1, repsBase + rule.reps.value * rounds); if (rule.weight?.type === 'increment') { if (weightBase !== null) weightTarget = Number((weightBase + rule.weight.value * rounds).toFixed(rule.weight.decimals ?? 1)); } else if (rule.weight?.type === 'percent') { if (weightBase !== null) weightTarget = Number((weightBase * (1 + rule.weight.value * rounds)).toFixed(rule.weight.decimals ?? 1)); } if (weightTarget !== null && Number.isNaN(Number(weightTarget))) weightTarget = null; return { ...exercise, setsTarget, repsTarget, weightTarget }; }
@Injectable()
export class WorkoutsService { constructor(private prisma: PrismaService) {} async today(userId: string) { const plan = await this.prisma.workoutPlan.findFirst({ where: { userId, isActive: true }, include: { days: { include: { exercises: true } } } }); if (!plan) return { status: 'no_active_plan' }; const weekIndex = calculateWeekIndex(plan as any); const dow = new Date().getDay(); const day = plan.days.filter((d) => Number(d.weekIndex ?? 0) === 0 || Number(d.weekIndex ?? 0) === weekIndex).find((d) => Number(d.dayOfWeek) === dow); if (!day) return { status: 'no_schedule', planId: plan.id, currentWeekIndex: weekIndex, plan }; const exercises = day.exercises.map((exercise) => applyProgression(exercise, weekIndex, plan.progressiveOverloadEnabled)).map((exercise) => ({ name: exercise.exerciseName, sets: exercise.setsTarget, reps: exercise.repsTarget, weight: exercise.weightTarget })); return { status: 'scheduled', planId: plan.id, planDay: day, plan, weekIndex, title: day.title, day: `Day ${day.dayOfWeek + 1} (${DAY_LABELS[day.dayOfWeek] ?? 'Day'})`, exercises }; } async current(userId: string) { return this.today(userId); } async start(userId: string, workoutSessionId: string) { const session = await this.prisma.workoutSession.findFirst({ where: { id: workoutSessionId, userId } }); if (!session) throw new NotFoundException(); return this.prisma.workoutSession.update({ where: { id: workoutSessionId }, data: { status: 'in_progress', actualDate: new Date() } }); } async complete(userId: string, workoutSessionId: string) { const session = await this.prisma.workoutSession.findFirst({ where: { id: workoutSessionId, userId }, include: { setResults: true, plan: { include: { days: { include: { exercises: true } } } } } }); if (!session) throw new NotFoundException(); if (session.status === 'completed') return session; await this.prisma.workoutSession.update({ where: { id: workoutSessionId }, data: { status: 'completed', completedAt: new Date(), actualDate: session.actualDate || new Date() } }); return { success: true }; } }
