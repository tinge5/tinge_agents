import React, { useState } from 'react';
import { View, Text, Pressable, TextInput, ScrollView } from 'react-native';
export function WorkoutDetailScreen({ route }: any) { const workout = route?.params?.workout ?? { title: 'Workout' }; const [completed, setCompleted] = useState(false); return <ScrollView contentContainerStyle={{ padding: 20, gap: 12 }}><Text style={{ fontSize: 28, fontWeight: '800' }}>{workout.title}</Text><Text>Quick logging UI optimized for mobile workouts.</Text><View style={{ gap: 10 }}>{(workout.exercises ?? []).map((e: any) => <View key={e.name} style={{ borderWidth: 1, borderColor: '#cbd5e1', padding: 14, borderRadius: 16, gap: 8 }}><Text style={{ fontSize: 18, fontWeight: '700' }}>{e.name}</Text><TextInput defaultValue={String(e.weight)} keyboardType="numeric" style={{ borderWidth: 1, padding: 12, borderRadius: 12 }} /><TextInput defaultValue={String(e.reps)} keyboardType="numeric" style={{ borderWidth: 1, padding: 12, borderRadius: 12 }} /></View>)}</View><Pressable onPress={() => setCompleted(true)} style={{ backgroundColor: completed ? '#15803d' : '#111827', padding: 18, borderRadius: 16 }}><Text style={{ color: 'white', textAlign: 'center', fontWeight: '800' }}>{completed ? 'Workout Completed' : 'Mark Completed'}</Text></Pressable></ScrollView>; }
